<Page x:Class="SupaTweaker.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="Transparent">
    <ScrollViewer>
        <StackPanel>
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Запуск" FontSize="18" FontWeight="SemiBold"/>
                    <DockPanel Margin="0,12">
                        <ToggleButton x:Name="AutoStart" Style="{StaticResource Switch}" DockPanel.Dock="Right" Checked="AutoChanged" Unchecked="AutoChanged"/>
                        <StackPanel>
                            <TextBlock Text="Автоматически запускать при включении ПК" TextWrapping="Wrap"/>
                            <TextBlock Text="В трее, с правами администратора (планировщик заданий)" Foreground="{DynamicResource BrushMuted}" FontSize="12" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DockPanel>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Темы" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Оформление окна. Выбор сохраняется." Foreground="{DynamicResource BrushMuted}" FontSize="12" Margin="0,6,0,12" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="ThemeStd" Style="{StaticResource Seg}" Content="Стандартная" GroupName="theme" Click="ThemeClick"/>
                        <RadioButton x:Name="ThemeDark" Style="{StaticResource Seg}" Content="Тёмная" GroupName="theme" Click="ThemeClick"/>
                        <RadioButton x:Name="ThemeLight" Style="{StaticResource Seg}" Content="Светлая" GroupName="theme" Click="ThemeClick"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="О SupaTweaker" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Margin="0,10,0,0" TextWrapping="Wrap"
                               Text="Версия 1.0.0 · .NET 8 WPF · Visual Studio Insiders&#x0a;&#x0a;Крестик скрывает в трей. Правый клик по значку — Выход.&#x0a;&#x0a;Активация Windows намеренно отсутствует. Перед массовыми твиками создайте точку восстановления."/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
